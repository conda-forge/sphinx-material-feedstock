{% set name = "sphinx-material" %}
{% set dunder_name = name.replace("-", "_") %}
{% set version = "0.0.33" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ dunder_name }}/{{ dunder_name }}-{{ version }}.tar.gz
  sha256: 282a92f0a83f1263a9553785b742fa83134d258fe20ae557df4cede4bd93770d

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3,<4
  run:
    - beautifulsoup4
    - css-html-js-minify
    - lxml
    - python >=3,<4
    - python-slugify
    - setuptools
    - slugify
    - sphinx >=2.0

test:
  imports:
    - sphinx_material

about:
  home: https://github.com/bashtage/sphinx-material
  license: MIT
  license_family: MIT
  license_file: LICENSE.md
  summary: Material sphinx theme
  doc_url: https://pypi.org/project/sphinx-material/#description
  dev_url: https://github.com/bashtage/sphinx-material

extra:
  recipe-maintainers:
    - mxr-conda
    - ndmaxar
    - oblute
    - rluria14
